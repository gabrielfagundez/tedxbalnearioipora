<%= render partial: 'shared/page_title', locals: { title: 'Clients' } %>

<section class="content">

  <div class="row">
    <% @clients.each do |client| %>
      <%= render partial: 'clients/client_box', locals: { name: client.name, url: projects_path(client_id: client.id), description: '', new: false } %>
    <% end %>
  </div>

</section>
